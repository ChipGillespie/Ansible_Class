---
- name: this playbook installs packages
  hosts: webServers
  become: true
  tasks: 
  - name: install package
    yum: 
      # name: ['git', 'make', 'gcc', 'wget', 'telnet', 'gzip']
      name: "{{  }}"
      state: installed
    with_items:
      - git
      - make
      - gcc
      - wget
      - telnet
      - gzip  
