---
- name: Install packages thru loop
  hosts: localhost
  become: true

  vars:
    packages: [ftp,telnet,htop)  

  tasks:
  - name: Install package
    yum
      name: ‘{{items}}’
      state: present
    with_items: ‘{{packages}}’
